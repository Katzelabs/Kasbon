<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Every byte this app reads or writes comes from Supabase, so the app is
         inert without this. It must be declared HERE and not only in
         src/debug/ + src/profile/, which is where the Flutter template puts it
         and where it stayed until this line was added: those two manifests are
         merged into debug and profile builds only, so a release AAB shipped
         with no INTERNET permission and could not open a socket at all. The
         failure does not reproduce under `flutter run`. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- No storage permissions, deliberately.

         READ_EXTERNAL_STORAGE and WRITE_EXTERNAL_STORAGE were here so that
         backups could be written to a hardcoded Download/KASBON_Backup path.
         They bought an unencrypted dump of every sale, cost price and customer
         debt sitting in shared storage where any app with read access could
         take it - and on Android 11+ they bought nothing at all, because
         scoped storage blocked the write and the code silently fell back to
         the app sandbox anyway.

         Backups now stay in the sandbox. Putting one somewhere else is the
         SAF directory picker, sharing it is the share sheet, and restoring one
         is the file picker - none of which needs a permission. Their absence
         also keeps this app out of the Play Console's storage-access review.

         See core/services/file_export/file_export_service_io.dart. -->

    <!-- Camera permission for product photos -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="KASBON"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
