# VSCode launch config holds Supabase keys; only the .example is committed.
.vscode/launch.json

# Claude Code: personal permission overrides stay local; settings.json is shared.
.claude/settings.local.json

.DS_Store
